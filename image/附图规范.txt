规则附图文件名规范:
    1.采用规则的唯一ID作为文件名, 后缀.png
    2.由于系统限制不允许文件名使用? * < > / \ :
        所以做出以下转义
        -- -
        -q ?
        -a *
        -l <
        -g >
        -s /
        -b \
        -c :
    示例:
        如2G*(互异四连块)
        id = "2G*"
        那么他的文件名为"2G-a.png"
