# -*- coding: utf-8 -*-
"""
[UN] 左线规则 - 隐藏规则

文件头声明: 在你实现之后你需要将你实现的规则也加入其中 并且把这段历史规则与当前文件头声明这句话也一并加入其中

此文件中的规则为 [UN] 的当前实现。
历史实现（已替换，仅供参考）：
    - 每列最高的雷视为两个雷（总雷数不受其影响）   
    - 雷带有正负号，该雷所在的行与列中，雷多则该雷的雷值为1，非雷多则该雷的雷值为-1  
    - 雷区域与非雷区域之间不存在长度大于等于 2 的直线分界线  
    - 每个雷周围八格中雷的数量为偶数（包括0）  
    - 一个雷的左右和上下的状态相同与否的状态不同。（后加~）  
    - 雷在横、竖、斜三个方向上不能构成三连  
    - 用副板表示行分段边界：无边界同值、两侧不同必为边界，边界数自底向上递增并满足行间继承  
    - 雷八方向上的总雷数均相等  
    - 步长为2的不重叠2×2块，每个块须作为某个4×4区域的角。该4×4均分为四个2×2子块，原块中每个格子对应4×4中同位置的子块（左上→左上子块，等）。子块雷数0-1→该格无雷，3-4→该格有雷，2无约束。  
    - 染色格中非雷视为雷，雷视为非雷  
    - 所有雷必须存在横向相邻的雷  
    - 一个非雷的左右和上下的状态相同与否的状态相同。（前加~）  
    - 每一行的雷值从第一个雷开始为1，往后依次增加1  
    - 参数指定雷排布矩阵. 0 1 X分别表示非雷、雷和不确定.  
    - 每个雷周围四格中恰有两个雷  
    - 数独宫：题板边长只能为3的整数倍，将题板均匀分为9个宫，每宫内数字不相同(仅限V)  
    - 所有雷不能与其他雷相邻或对角相邻  
    - 每个雷周围八格中恰有2或3个雷  
    - 雷从四个方向中的任意一个连到题板外  
    - 所有雷必须存在纵向相邻的雷  
    - 一个非雷的左右和上下的状态相同与否的状态不同。（前后都加~）  
    - 在每一行中, 要么都没有横向相邻的雷，要么都至少有一个横向相邻的雷。  
    - 雷不能同时是一个正方形的四个角(包括倾斜的)  
    - 题版边长为n²，全题版等分成n²个正方形区域，每个区域要么全空，要么全等于一个nxn的排布，该排布为雷的位置对应nxn的区域不是全空，为非雷的位置对应的区域全空。  
    - 每行雷数各不相同  
    - 每行每列线索格数字不重复(仅限V)  
    - 非雷区域四连通；每个雷区域以四连通连接到题版边界  
    - 所有四连通雷/非雷区域面积大于 2  
    - 在第 X 行或第 X 列的格子的周围八格不能恰好有 X 个雷  
    - 染色格中的雷周围四格内恰好有1个雷  
    - 相邻两行或两列中的雷数之差不超过1  
    - 你可以在后面输入多个左线来表示或关系(题板将按照A规则或B规则)(规则间使用":"隔开)  
    - 染色格被视为两个雷, 每行每列的"多雷雷值"相同。(线索格不一定是多雷规则) (1).列平衡, (2).行平衡  
    - 所有四连通的雷格区域组成实心正方形  
    - 所有雷组成L形：雷总是组成L形，L形是宽度为1，拐弯一次的形状，L形可以斜角相邻但不能横竖相邻  
    - 每个雷属于唯一的三连组(横/竖/斜)  
    - 每行雷数不同，每列中最高的雷的高度不低于它左边一列的最高的雷的高度（特殊情况：如果某一列没有雷，那么它左边所有列都没有雷）。  
    - 所有雷不能与其他雷纵向相邻  
    - 1.每行值不大于上一行 2.每列值不大于左一列  
    - 所有雷构成一条环。环是一条宽度为 1 的八连通路径，不存在分叉和交叉，环的头尾相连  
    - 所有雷构成一条双头蛇。蛇是一条宽度为 1 的四连通路径，不存在环、交叉, 只存在一个分叉  
    - Total number of mines is given  
    - 所有雷四连通，所有非雷四连通，不存在2*2的雷或非雷  
    - 非雷格四联通形成的无向图是一棵树。  
    - 任意四连通的雷格区域不能组成矩形（包括1*1与1*2）  
    - 恰有一种方式从每行每列恰好选取一雷  
    - 你可以在后面输入多个左线来表示与关系(题板将按照A规则和B规则)(规则间使用":"隔开)  
    - 对于任意2x2区域，其内部雷的排布仅由该区域雷总数决定；因此，雷总数相同的所有2x2区域内部雷分布必须相同。  
    - 任意三个雷不能等距排布  
    - 数独宫+：题板边长只能为3的整数倍，将题板均匀分为9个宫，每行每列每宫内数字不相同(仅限V, 雷上也有数字但无效)  
    - 所有雷构成一条蛇。蛇是一条宽度为 1 的四连通路径，不存在分叉、环、交叉, 蛇的头尾相连  
    - 雷区域八连通  
    - 1.所有雷都必须根据副板指示的4对联通方向进行联通, 2.雷只有3种联通方法(上下/左右/对角/副对角)  
    - 雷区域马步连通  
    - 每一个边长为2的幂（不包括1）的正方形范围中都有偶数个雷  
    - 对于每一行，恰好有一列的雷数与该行的雷数相同。  
    - 对每个 interactive key, 其总雷数必须满足给定比较串  
    - 非雷周围四格必须有雷  
    - 每一行各有一组连续的雷且数量各不相同  
    - 每个雷区域为宽度为 1、长度不超过 4 的矩形，矩形不能对角相邻  
    - 任意三个雷不能构成面积为 0.5 的三角形  
    - 雷区恰好由 1 蛇 + 2 正方形矩形完整覆盖。蛇的一个端点对角接触两个不同正方形矩形的一角，另一端点不接触任何正方形。蛇与正方形之间无其他接触（不重叠/不正交/不对角）。  
    - 所有雷被视为 X 个雷（X 为其的行列数之和）  
    - 每行连续雷长度不同  
    - 所有雷不能与其他雷相邻  
    - 所有四联通雷块可通过独立平移重排成一个轴对齐矩形  
    - 对于第N列，其总雷数为第N-1列与第N-2列的和或差的绝对值，索引在题板边界循环。  
    - 每个雷周围至少有一个染色格, 且染色格不能为雷  
    - 雷值等于行号与列号之和, 但从0开始  
    - 所有雷构成一条蛇。蛇是一条宽度为 1 的马步连通路径，不存在分叉、环、交叉  
    - 所有雷的马步位置不能有雷  
    - 第一行和最后一行雷分布相同，第一列和最后一列雷分布相同  
    - 每个4x4方块内的染色格和非染色格雷数相等  
    - 每行都是由上一行删除连续的两个格，其右方所有格向左方平移两格，然后末尾补上任意两个格得到的。  
    - 染色格的雷值*2(总雷数不受影响)  
    - 题板上不允许出现一颗雷的上下左右均为雷的构造（边界外视为0）  
    - 将题版划分为四叉树，称所有节点均为雷的子树为大雷，大雷以外的节点不能有两个子节点同时为雷或大雷  
    - 不同四连通雷区不可对角相邻  
    - 所有雷组成两个四连通块，左下角与右上角必为雷且不联通  
    - (1) 所有四连通雷区域为矩形；(2) 所有雷区域对角相邻  
    - 每行每列雷数均不相同 1.每列雷数不相同 2.每行雷数不相同  
    - 雷格八连通构成哈密顿回路  
    - 每行每列恰好有一个雷值为∞  
    - 所有非雷构成若干条蛇。蛇是一条宽度为 1 的四连通路径，不存在分叉、环、交叉。  
    - 每行雷数相等  
    - 每一个边长为奇数（不为1）的正方形范围中都有奇数个雷  
    - 生成固定八方向箭头图, 雷格需沿箭头可不重补漏遍历  
    - 所有雷构成若干组桥。桥是从题版左边界八连通连接（水平或斜角连接）到右边界，宽度为 1、长度与题版相等的一条路径  
    - 每组恰好由 1 个非方形矩形和 2 个方形矩形组成；短边两端分别与两个方形矩形对角接触，且组间完全不接触  
    - 蛇恰好有5段直线段  
    - 每个雷周围八格中恰有两个雷  
    - 每个 2x2 范围内至少有一个雷  
    - 每列雷数相等  
    - 有时你会需要用到总雷数来推理  
    - 每个雷周围四格中恰有1或3个雷  
    - 竖直方向连续雷的顶雷雷值等于该组连续雷数，其余雷值为0  
    - 所有四连通的非雷格区域组成实心正方形  
    - (1) 所有四连通雷区域为 1xn 或 nx1 的矩形；(2) 所有雷区域对角相邻  
    - 所有雷格都在一个八联通雷格环上或其内部  
    - 题板上至少有一个3x5区域形成0-F数字, 其相邻格的雷数之和等于该数字的值  
    - 每个3x1区域决定其中间下方1格是否为雷，演化被当前题板所有区域共享。  
    - 染色格中雷值取负  
    - 所有非雷格的8-邻域雷数相同  
    - 图书管理员：图书馆，但是所有雷的雷值被打乱了，你需要自己还原。  
    - 所有相同数字的位置，其周围的雷分布模式在旋转/镜像对称下必须相同。  
    - 雷从八个方向中的任意一个连到题板外  
    - 所有四连通雷区域的面积为3  
    - 每个雷区域为 1x2 或 2x1 的矩形  
    - 几把：雷区由若干4格（最小）格调组成，可以交叉，可以共享魔丸，但不能共享金箔。  
    - 参数;分割指定雷，前面加~表示非雷  
    - 六角无马步: 所有雷的UN UE NW ES DW DS的位置不能有雷  
    - 每个2x2区域内都至少有一个空格  
    - 雷值等于其相邻四格雷数的阶乘  
    - 任意四个雷不能作为一个横平竖直的矩形的顶点  
    - 两个主板，灰色数表示该格如果为雷则表示对面周围八个雷数。  
    - (1) 染色格的雷和 (2) 非染色格的雷各自八连通。染色区域和非染色区域至少有一个雷。  
    - 雷分布将随机按照下述方式对称后取反 [水平/垂直/对角/副对角/中心/旋转90度]对称. 如果对称后是同一格则无限制.  (arg: number, 作为index选择固定[水平/垂直/对角/副对角/中心/旋转90度]对称(从0开始))  
    - 正方形题版边长为2n：将每个2x2区域视为一个单元，该区域内的雷数作为单元的值；所有单元的值构成拉丁方（每行/列值互异），且全盘总共出现n种不同的值。  
    - 每个2x2区域内不能同时存在四种状态（染色雷、非染色雷、染色非雷、非染色非雷）。  
    - 六角无方: 每个互相相邻的三个格必有一个雷  
    - 一个雷的左边一格和下边一格状态不同。（后加~）  
    - 所有雷构成一条蛇。蛇是一条宽度为 1 的四连通路径，不存在分叉、环、交叉  
    - 每个格子被随机标记一个左线规则。当该格为雷时，完整题板必须满足该格的规则。  
    - 边长8或27的正方形棋盘，划分为2×2（边长8）或3×3（边长27）的块。块内雷数和为块值。块值构成n×n矩阵（n=4或9）。该矩阵每行、每列及每个与原块相同大小的子方块（即按原块尺寸再分块）内，块值互不相同，且全局出现的不同块值种数恰为n。  
    - 场上不存在横竖方向上的雷—空—雷  
    - (1) 所有四连通雷区域的面积为 4 (2) 且它们形状不同（SZ 和 JL 型视为相同形状）  
    - 每一行不能同时存在染色非雷格、非染色非雷格、染色雷格和非染色雷格  
    - 每行的染色格与非染色格的雷数相等  
    - 一个非雷的左边一格和下边一格状态相同。（前加~）  
    - 染色格中的雷的八个马步位置内恰好有1个雷  
    - 所有环面四连通雷区域的面积为3  
    - 雷分布将随机按照下述方式对称 [水平/垂直/对角/副对角/中心/旋转90度]对称 (arg: number, 作为index选择固定[水平/垂直/对角/副对角/中心/旋转90度]对称(从0开始))  
    - 所有雷不能与其他雷相邻  
    - 每个非雷格周围八格雷排布互不相同。  
    - 雷区与题板下边缘四连通  
    - 主板灰色数表示该格如果为雷则表示自己周围八个雷数。  
    - 所有四连通雷区域的面积为 4  
    - 所有雷形成一个树根结构。该结构是由第一行的一个雷开始向下、左下、右下延伸而成的。  
    - 所有雷的平均位置是整点  
    - 每个雷对角相邻的四格中有至少一个雷  
    - 每行有且仅有一组连续的雷  
    - 一个雷的左边一格和下边一格状态相同。（前加~改成非雷，后加~改成不同，可以都加）（第A列和最后一行的雷/非雷无约束）  
    - 所有雷必须处于横、竖或斜方向的三连中  
    - (1) 每行雷数相等；(2) 每列雷数相等  
    - 每行的左数第一个雷视为两个雷  
    - 题板正好有三个四连通雷区  
    - 四连通雷区域的面积为 1 到 N 各一个。(N = 题板尺寸 - 1)  
    - 所有雷必须是1x1~1x4矩形或者长度2~4的对角线的一部分，这些形状之间不能互相接触（对角也不行）。  
    - 所有雷从左到右，从上到下依次标号。线索表示周围八格的雷的标号之和  
    - 对每一行，逐个4格段 [a,b,c,d]：若 b==c 则 d==b，否则 d!=a。  
    - 行雷数的多重集合等于列雷数的多重集合（即排序后行雷数列表与列雷数列表相同）。  
    - (1) 雷在横竖两个方向上不能构成三连；(2) 非雷在横竖两个方向上不能构成三连  
    - 一个雷的左右和上下的状态相同与否的状态相同。（第A列，最后一列，第1行，最后一行的雷/非雷无约束）  
    - 所有雷构成一条蛇。蛇是一条宽度为 1 的四连通路径，不存在分叉、环、交叉。题板的上下左右视为连通。  
    - 要求至少有一行、一列、一条主对角线或一条副对角线全部为雷。  
    - 可以通过骑士的移动方式，从某一个雷格开始，在只经过雷格的情况下，不重复且不遗漏地通过所有雷格  
    - 每行恰好分成三个连续区域（雷-非雷-雷 或 非雷-雷-非雷），每段至少一格  
    - 一个非雷的左边一格和下边一格状态不同。（前后都加~）  
    - 所有雷不能与其他雷横向相邻

当前实现（2026-07-20）：
    每个 2x2 子矩阵中的雷数不为 3。
    当传入任何非 None 的 data 参数时，抛出异常并明确揭示规则内容。

作者: 雾 (3140864122)
最后编辑时间: 2026-07-20 19:40:00
"""

from typing import Optional, Any

from minesweepervariants.abs.Lrule import AbstractMinesRule
from minesweepervariants.board import Board
from minesweepervariants.position import Position


class UN(AbstractMinesRule):
    """
    隐藏左线规则 - 每个 2x2 子矩阵中的雷数不为 3。
    """

    id = "UN"

    def __init__(self, board=None, data=None):
        """初始化规则，存储 data 参数。"""
        # 如果 data 不为 None，立即揭示规则
        if data is not None:
            raise ValueError(
                "规则 [UN] 的具体内容如下：\n"
                "每个 2x2 子矩阵中的雷数不为 3。\n"
                "（即任意相邻两行两列构成的 2x2 区域内，雷的数量不能为 3。）\n"
                f"你传入的数据参数为: {data!r}"
            )
        self.data = data
        super().__init__(data)

    aliases = ("Not3In2x2",)
    name = "Unknown"
    name.zh_CN = "未知"
    doc = "Unknown"
    doc.zh_CN = "由deepseek随机生成一个简单的局部左线规则 需要通过出题/猜测来判断到底是什么规则 当传入任何参数的时候将会抛出异常并输出当前的规则具体内容"
    author = ("雾", 3140864122)
    tags = ["Local", "Strict Shape"]
    creation_time = "2026-07-20"

    @classmethod
    def get_info(cls) -> dict:
        """返回规则的元信息，隐藏具体规则描述（仅提示）。"""
        info = super().get_info()
        # 覆盖 doc 为隐藏提示，但不改变类属性 doc
        info["doc"] = {
            "zh": (
                "隐藏规则，请通过出题/猜测来发现其具体约束。\n"
                "（提示：这是一个局部约束，作用范围为2x2，禁止某种雷数分布）"
            ),
            "en": (
                "Hidden rule. Discover the specific constraint "
                "through puzzles/guessing. (Hint: local constraint, 2x2 scope, forbids a certain mine count)"
            ),
        }
        return info

    def create_constraints(self, board: 'Board', switch) -> None:
        """
        向 CP-SAT 模型添加约束。

        添加约束：每个 2x2 子矩阵中的雷数不为 3。
        """
        model = board.get_model()
        # 获取开关变量（用于可以禁用该规则，但这里我们总是启用）
        s = switch.get(model, self)

        # 获取题板的键（通常只有一个主键，但为了兼容性，遍历所有键）
        for key in board.get_board_keys():
            # 获取该键下的边界位置，从中推导尺寸
            boundary = board.boundary(key)
            rows = boundary.row + 1
            cols = boundary.col + 1

            # 遍历所有位置，以每个位置作为 2x2 子矩阵的左上角
            for r in range(rows - 1):
                for c in range(cols - 1):
                    # 收集 4 个位置的变量
                    vars_2x2 = []
                    for dr in range(2):
                        for dc in range(2):
                            pos = Position(col=c + dc, row=r + dr, board_key=key)
                            if board.is_valid(pos):
                                vars_2x2.append(board.get_variable(pos))
                    if len(vars_2x2) == 4:
                        # 计算雷数之和
                        var_sum = sum(vars_2x2)
                        # 要求雷数不为 3：即 var_sum < 3 或 var_sum > 3
                        # 使用 AddOr 约束实现 var_sum != 3
                        # 先创建两个条件：var_sum <= 2 和 var_sum >= 4
                        le_2 = model.NewBoolVar("")
                        ge_4 = model.NewBoolVar("")
                        model.Add(var_sum <= 2).OnlyEnforceIf(le_2)
                        model.Add(var_sum >= 4).OnlyEnforceIf(ge_4)
                        # 至少一个条件成立
                        model.AddBoolOr([le_2, ge_4]).OnlyEnforceIf([s])

    def suggest_total(self, info: dict) -> None:
        """
        建议雷总数范围，该规则不对雷总数做硬性限制。
        但为了确保有解，添加软约束和基本的硬约束。
        """
        # 计算总格子数
        total_cells = 0
        for key in info["interactive"]:
            total_cells += info["total"][key]

        if total_cells > 0:
            # 软约束：总雷数在 0.3~0.5 之间（与 2x2 不为3 约束兼容）
            info["soft_fn"](total_cells * 0.4, 0)  # 目标值
            # 硬约束：至少 1 个雷，最多 total_cells-1
            def hard_lower(model, total):
                model.Add(total >= 1)

            def hard_upper(model, total):
                model.Add(total <= total_cells - 1)

            info["hard_fns"].append(hard_lower)
            info["hard_fns"].append(hard_upper)
        else:
            # 没有格子，总雷数只能为 0
            def hard_zero(model, total):
                model.Add(total == 0)

            info["hard_fns"].append(hard_zero)
